name: "(Deprecated) Fetch ISTAT Population"

# DEPRECATED — Superseded by fetch-tier1.yml which handles all Tier 1 data.
# Keeping this file to avoid breaking any existing references.
# Use fetch-tier1.yml or manual trigger with fetch_population=true instead.

on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use fetch-tier1.yml instead."
        type: string
        default: "Use fetch-tier1.yml"

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "⚠️ This workflow is deprecated."
          echo "Use 'Fetch Tier 1 Data (API-Fed)' workflow instead."
          echo "It fetches population + demographics + boundaries + recomputes derived indicators."
